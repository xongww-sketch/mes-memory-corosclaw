# 2026-06-16（周二）Daily Notes

## 📊 概览

- **总消息数**：217 | **用户提问数**：25 | **session 数**：4
- **核心议题**：n8n 全链路数据完整性论证 → 方案 A（重写工作流，单表事实源）→ coros 后台推送架构设计

---

## 🔍 n8n 全链路数据完整性论证

**发起人**：熊旺
**目标**：确保一条 retroid 的所有信息（包装、生产、渠道、wifimac、电池、屏幕、组件、uuid）进入 daily_info 时一个都不少。

### 熊旺的核心要求

1. **宏观视角**：先看 mes.mes_dailydata 库下所有表被哪些工作流使用（插入和查询），再定位问题
2. **数据完整性**：一条 retroid 到了 FQC、包装完成后，所有信息必须正确插入；后续有了渠道也要能更新
3. **先理清楚再动手**：暂时不 push 到 coros 后台，先把数据逻辑理清楚

### 渠道数据源纠正（⚠️ 重要修正）

熊旺纠正了虾王对渠道数据源的理解：

| 数据源 | 来源 | 目标表 | 说明 |
|--------|------|--------|------|
| **国内渠道** | 管易 API → mes_delivery_order_details | Postgres | 国内发货渠道汇总 |
| **国外渠道** | OMS → MongoDB snoutbounds 集合 | MongoDB1 节点 | 国外发货渠道汇总 |

**关键纠正**：
- ~~daily_info 的渠道以旧 MES consm_barcodebindinfo 为准~~ ❌ 错误
- **正确理解**：有了生产数据后，从管易和 OMS 两个源拉渠道数据，给每个 SN 补上渠道
- 两源都查，**不区分国内外**（daily_info 不分国内外机型），谁有用谁
- 渠道数据**只更新 daily_info 和旧 MES 的渠道字段**
- 渠道**每晚补**即可；如果没有生产数据要报警提示（渠道都有了不可能没有 SN 信息）
- **回写旧 MES 必须保留**

### 涉及表确认

熊旺要求确认所有涉及 mes_dailydata 库表的工作流，最终确认 8 个工作流（排除手动补 SN 渠道、定时器测试、Redis 扫描任务）。

---

## 🏗️ 方案 A：重写工作流 + 单表事实源

**熊旺决策**：选方案 A — 直接重写全部工作流，不被现有逻辑限制。

### 核心设计

- **单表事实源**：daily_info 作为唯一事实表，不再维护多表
- **数据去重迁移**：现有 daily_info 有 SN 重复多条数据，需整理成新表，旧表改名备份
- **重写工作流**：用旧逻辑作参考，但直接重写，考虑后期可维护性、扩展性、稳定性
- **所有操作在 n8n 完成**：接口逻辑写在 n8n 工作流 JSON 里

### 架构分层

```
采集子流（管易 + OMS 渠道）
    ↓
写入编排（组装 daily_info 完整行）
    ↓
推送 coros（完整记录推送）
    ↓
监控（失败重试 + 报警）
```

---

## 🔄 coros 后台推送架构设计

### 双通道设计

**1. 定时同步通道**（已有，保留优化）
- 渠道每晚批量补数据
- 生产数据定时同步
- 加状态登记 + 幂等 upsert

**2. 实时推送通道**（新增）
- **绑定/解绑实时推送**：工序 39（绑定）和工序 62（解绑）末尾挂 HTTP 节点 → coros 实时 PUSH
- **频率**：3 分钟一次批量推送（熊旺确认）
- **原因**：coros 以 uuid 作为锁机判断条件，绑定/解绑变化需及时更新，避免用户激活时被误锁机
- **日量**：5000+ 次绑定/解绑，批量推送比逐条推送更轻量

### coros 完整记录推送接口（王志坚提供）

**接口凭据**：
- URL: `POST https://simpleadmincntest.coros.com/coros/admin/product/mes/push`
- Apifox: https://app.apifox.com/link/project/4564505/apis/api-436001509

**签名算法**（SHA1 40 位 hex）：
```
nonce     = 9 位随机数 (100000000~999999999)
timestamp = 秒级时间戳 (System.currentTimeMillis()/1000)
secret    = "7mQxK2vNc8LpHs4Tg9YdR1bWf6JzUaEe"  (mesProductPushSignToken)
li = [nonce, timestamp, secret] → Collections.sort(字典序)
signature = DigestUtils.sha1Hex(li[0]+li[1]+li[2])
```

**协议规则**：
- Header: signature + timestamp + nonce + Content-Type: application/json
- Body: `{"msg":[{完整 daily_info 行}]}`，msg 数组可批量
- 按 retroid 去重
- retroid/uuid/createtime 不能为空
- 返回 result=0000 成功

**daily_info 完整字段**（= 推送 body 字段）：
id, retroid, production_id, delivery_id, machine_type, job_number, uuid, phase, wifi_mac, battery, screen, remark, channel, createtime, production_createtime, delivery_createtime, packing_createtime

### 锁机推送接口（待 coros 提供）

- **独立于**完整记录推送接口
- coros 后台还没给接口文档
- 需要根据我方实际，写一个规范让 coros 遵照实现
- 高频 3 分钟推送 uuid 绑定状态

---

## ⛔ 硬规则确立（熊旺强制要求）

**查 n8n 工作流，永远只用 n8n API key 调 Public API 拉实时最新定义，绝不看本地 zip/备份/缓存。**

- 原因：本地备份会过时，曾因用过时备份导致改造错位
- references/n8n/*.zip 和 all-workflows 等本地副本：**仅作历史参考，绝不作为改造基线**

---

## 📦 交付物整理

熊旺要求将所有文件打包 zip 交付，包括：
- 改好的 n8n 工作流 JSON（每个工作流独立文件）
- 整合 daily_info 数据的 SQL
- 存储过程 SQL
- 推送 coros 的 JS 代码（写在工作流里）

**熊旺强调**：发文件前先从宏观思考好执行步骤，确认符合自己设计的架构再发，不要乱、不要模糊、不要幻想。

**country 字段确认**：daily_info 存在 country 列，需检查所有交付文件是否需要变动。

---

## 📋 待办事项

- [ ] 完成方案 A 全部工作流 JSON 重写（基于 n8n API 拉最新定义）
- [ ] daily_info 去重迁移 SQL（新表 + 旧表备份）
- [ ] 存储过程 SQL
- [ ] 推送 coros 的 JS 代码（SHA1 签名 + HTTP 推送）
- [ ] 全部文件打包 zip 交付
- [ ] coros 锁机推送接口规范撰写（给 coros 后台参照实现）
- [ ] 确认 country 字段在所有交付文件中的一致性

---

## ⚠️ 重要教训

1. **渠道数据源理解偏差**：之前误以为渠道以旧 MES 为准，实际是管易（国内）+ OMS（国外）双源补数据
2. **工作流必须基于最新生产定义**：本地缓存的 n8n.zip 不是最新，导致交付的工作流 JSON 与生产不一致
3. **多工作流 JSON 必须拆分**：每个工作流独立一个文件，不能混在一起


---

## 自动提取的对话摘要

# 2026-06-16 (周二) Daily Notes

## 今日对话摘要

- **总消息数**：225
- **用户提问数**：25
- **session数**：4

## 用户提问/指令记录

### 03:18

现在请你回忆，我们这两天无论如何，要把跟coros后台数据对接的全流程，全部修复好，请回忆

### 03:40

你是基于最新的n8n的工作流去查询的嘛？请不要把你本地n8n的备份作为参考，然后现在我有点模糊了，你的计划是怎样的，我想看清楚

### 03:47

暂时先不push到coros后台，暂时先理清楚，上周五你理清楚，出了这个引用的图，你看看分析的结果对不对，然后我建议你最好还是通过n8n的接口查询下，看看daily_info和其他几个附属表他们共属于mes.mes_dailydata这个库里面，这里面这些表，被哪些工作流使用了，插入和查询，所以你需要宏观得看，然后结合我的问题，保证一条retroid的所有信息，包装，生产，渠道，和一些wifimac，和组件信息，一个都不能少，必须要有，也一定会有，

### 06:39

我最后就是需要去解决现在的数据错乱的情况，你得看看问题在哪，然后再看怎么改，看看涉及到这几张表的数据，我最后只需要保证mes.mes_dailydata下的库表里的数据是正确的，能保证生产的数据，如果到了fqc，保证包装完成后，也能正确插入，后续如果有了渠道，也能放到马上更新，或者其实不需要这么复杂得多表存储，直接存储到daily_info ？最后只要保证一条retroid在daily_info的所有信息，，一个字段都不能少，必须要有，也一定会有，请你宏观思考，细致发现细节点数据逻辑问题

### 07:08

我觉得你的方案挺对的，这样就不会缺失数据，那我以后如果是实时推送数据给coros后台，应该怎么做呢，他们其实是需要最新的生产数据，以便于做一些锁机和激活的校验，这一块你要整体设计一下，留一个口子，是给coros后台推送数据的，数据维度就是daily_info的一行数据，如果有，就推，只要更新了，他们那边就得更新，而且最好留痕，保证一些推失败的能重试，接下来，请你给我个总方案，要怎么做，需要清洗一点不要废话

### 07:11

OMS 出库机渠道，不是在genchannel里会查出来，进行渠道数据和生产数据对比组装修改daily_info嘛？你有没有认真看最新的工作流，是不是记忆混乱了，定时器测试这个工作流你不用管它是用来测试的，先确认这几个店，再推进计划

### 07:16

跟这个些表
/home/node/.openclaw/media/inbound/a1013978-8e93-478d-928d-b88537db18ce.jpg
有关系的工作流，你确定只有这几个工作流吗，请继续确认

### 07:26

1. 8个工作流，手动补SN渠道信息不管，定时器测试不管，定时扫描Redis中同步任务执行情况这个不管，只要n8n所有工作流，没有其他工作流涉及这几张表的话，那就没有了，你去确认一遍
2. 这个取决于你需不需要去维护四张表，还是只维护一张daily_info表就够了
3. 你可以继续挖一下

因为管易会拉取第三方的渠道数据信息进来，涉及到mes_delivery_order_details表的工作流，也要注意，因为这是渠道的一个数据源，还有个数据源是oms的，所以这个上游数据，你也得保证好才行，要一起考虑

### 07:39

1. 同意单表事实源，但是现在的表，有sn重复多条的数据，是不是给我一个sql，把这个表的数据整理成新的daily_info表，然后把这个表，改名为备份表，这样有风险吗？
2. mes_delivery_order_details这个，你去get_channel里确认一下，是不是已经查了，你还是有查询的数据遗漏，上下文不全

你还有一个需要考虑的点，就是工作流还需要回补旧mes的数据的，这一块，一定要仔细看看，确保这块也不要出问题，请你认真分析，别啰嗦，考虑到一起
然后有没有考虑重新搭建一个架构，可以用之前的旧逻辑作参考，但是可以直接重写，同时你要考虑好后期的可维护性，和拓展性，和稳定性

### 07:45

daily_info 的渠道以旧MES consm_barcodebindinfo 为准
这个是不对的，
mes_delivery_order_details是国内发货的渠道汇总
oms是国外发货的渠道汇总，你忘了我告诉你的了吗？
我只是在有了生产数据后，把渠道数据从管易和oms拉去过来，给每个sn补上，这样才对

请你重新全部整理一遍，给我最终方案，还有不清楚的先找我确认

### 07:49

请你记住，以后所有让你查n8n工作流，只用api key，去调用n8n最新的工作流查询，不要再看自己本地存的zip文件了，请你记住！

### 07:55

1. getchannel的MongoDB1节点你读到了嘛？就是通过这个节点拿oms的信息的
2. 对的没错
3. 不对，渠道每天晚上补就行，如果没有生产数据，应该要做提示，因为渠道都有了，不可能没有sn信息
4. 两个源都得查，不用区分国内外，因为对于daily_info来说，他部分国内外机型，但是需要主要，渠道只会去更新daily_info和旧mes的渠道数据，这个是要记住的
5. 回写旧mes是要保留的，原因我上面说了

请你读完我的问题后，思考一下，要不要直接重写全部工作流，不要被现有的限制了，请你深度思考

### 08:08

方案A可以，然后还有一个地方要考虑，如果以后他们需要的是一些实时数据，在这个架构上，我要怎么给他们推呢？他们主要需要的，就是生产的实时数据，因为只有实时更新好了sn和uuid的绑定关系，而且实时更新，coros后台在校验锁机等操作，才能更准确，这个你怎么考虑的，我现在确认了你的A方案，然后现在我列出的这个需求，你可以从n8n和整个架构角度看看，是单独去处理，还是通过这样处理，因为如果实时上传，那就涉及到工序了

### 08:13

绑定的数据实时退过去了，那解绑的呢？因为某个机子有可能上午绑定了新的uuid,但是下午可能检测出有问题，需要解绑，这个怎么处理呢？你从架构角度去思考

### 08:24

你之前的方案很好，你不要忘了，有一个考虑点要跟你提一下
因为一天有可能会进行5000次以上的绑定和解绑，这样如果又有绑定，又要解绑，然后还要实时去推，会不会不好维护且感觉做得很重，有没有别的方法解决业务问题，你可以从业务角度出发，主要是为了校验住锁机状态，因为coros是以uuid作为锁机判断条件，如果uuid本来是绑定a这个sn,然后在他们晚上同步最新的数据前，这个sn解绑了原先的uuid，然后绑定了新的，在用户激活的时候，coros后台就会认定这个uuid非法绑定了新的sn,就会被锁机，所以如果能有效更新数据到他们那边，就能解决这个问题，所以你原先同步的方案很好，不要忘了，但是也要同时解决这个问题

### 08:34

1. 3分钟一次吧
2. coros接受，最后一次批量会好些
确认好了，现在，请你把同步coros定时同步的逻辑，和这个实时推送的逻辑，总结一下实现步骤和实现计划吧

### 08:54

- coros 实时推送接口文档（URL + SHA1签名规则 + secret）— R3 真正推送的前置
我会发聊天记录给你，稍等会发给你
- daily_info 去重迁移的写流停跑窗口（哪个时段安全切换）
现在就行
- coros 锁机推送接口是否独立于完整记录推送接口（两个口还是一个口）
独立于完整记录的推送接口，这个接口coros后台还没给，可以根据我们的实际，写一个让他们遵照的接口去写

### 08:55

<forwarded_messages>
[2026-03-31T14:46:04+08:00] 王志坚:
 产品数据推送的接口协议
 https://app.apifox.com/link/project/4564505/apis/api-436001509
[2026-03-31T14:46:39+08:00] 王志坚:
 curl --location --request POST 'https://simpleadmincntest.coros.com/coros/admin/product/mes/push' \
 --header 'signature: 6a41c3315b1d9985b2b7e5aebfc76f1951d11ea4' \
 --header 'timestamp: 1774939492' \
 --header 'nonce: 579256059' \
 --header 'Content-Type: application/json' \
 --data-raw '{
 "msg": [
 {
 "id": 1140211,
 "retroid": "...

### 08:59

你能不能根据你现在改造dailyinfo这些工作流的计划，把你列的计划的所有文件，整理成一个zip，给我，然后我来操作，记住，所有的操作都在n8n操作，所以如果接口有什么东西需要你做，你得写在n8n的json里面，

### 09:07

我说了，现在先不整理锁机推送这块绑定和解绑信息推送，先整理好这个方案a的落地文件zip,和实现步骤，我要按照你的计划去操作

### 09:14

改工作流的操作，你不直接给我工作流json，我怎么改，我直接贴一个新的，旧的不要了，全部重新写，不要旧的了，你确认好，请你重新给我文件，然后pushcoros那个js,肯定是写在工作流里面的，你忘了你的架构嘛？https://applink.feishu.cn/client/message/link/open?token=Ammmua…采集子流 + 1个写入编排 + 1个推送 + 1个监控，请认真细致，确认了是我需要的东西，才整理好给我

### 09:18

完全清楚了。两个渠道源在 Merge1 汇合，证实了你的纠正完全正确。我现在把正确理解钉死，回答你的"要不要重写"，然后给方向。
✅ 渠道双源真相（实证，对齐你的纠正）
国内: 管易 → mes_delivery_order_details → Postgres1 ─┐
国外: OMS → MongoDB snoutbounds → MongoDB1 ─┤
 (Merge1汇合)
 ↓
 两源都查，不区分国内外，谁有用谁
 ↓
 插入到每日出库表(delivery_detail)
 ↓
 回查生产数据组装 → daily_info
 ↓
 回写旧MES consm_barcodebindinfo(保留)




你的 5 点我全部确认理解：

1. ✅ OMS 国外渠道 = MongoDB snoutbounds 集合（我之前漏的 MongoDB1 节点，现已读到）
2. ✅ 国内 = 管易 → mes_delivery_order_details
3. ✅ 渠道每晚补；没生产数据要报警提示（渠道都有了不可能没SN）
4. ✅ 两源都查，不区分国内外；渠道只更新 daily_info ...

### 09:23

推荐x那就x，然后你要把完成你整个架构需要跑的s现在要整合daily_info数据的sql，存储过程的sql,等等都给我，还要把改好的n8n工作流json，也都给我，把文件打好包，我来执行，考虑的时候写的时候，请先从宏观思考好执行步骤，再操作，最后发给我前，请确认好是符合你自己设计的架构的，然后在发给我，不要乱，不要模糊，不要幻想

### 09:28

我选1，请你开干

### 10:01

country字段在daily_info存在列，有哪些你给我的文件需要变动

---
*由 cron 任务自动生成于 2026-06-16 13:01 UTC*
*数据来源：本地 session 文件 (/home/node/.openclaw/agents/main/sessions)*