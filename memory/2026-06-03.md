# 2026-06-03（周三）

## 🏗️ MES→coros后台 同步架构重设计（今日核心任务）

**发起人**：熊旺
**角色定位**：虾王以架构师身份重新设计同步架构
**目标**：解决长期存在的"漏同步/渠道空/wifimac空/无法重试/无法及时发现/实时性不足"问题，保稳定 + 提扩展

### 现状痛点根因诊断

用一句话概括：**用"定时批量快照"承担了本应"事件驱动 + 状态机 + 对账补偿"的职责。**

| 现象 | 根因 |
|------|------|
| 产品数据漏同步 | 定时任务跑的瞬间源数据未到位，过后无补偿 |
| 渠道号为空 | 渠道依赖"包装后才产生"，但定时任务不感知时序 |
| wifimac为空 | 生产工序42尚未完成就被同步，取到null后不再回补 |
| 无法重试 | 同步是"跑一遍"，失败/空值不记录、不排队 |
| 无法及时发现 | 没有对账与告警，靠人工导Excel发现 |
| 锁机校验失效 | uuid-sn绑定靠定时同步，出厂前coros后台还没收到 |

### 现役 6 工作流盘点

收到熊旺发的 zip 包后，完整分析了 6 个工作流，复制到 `workspace/references/n8n/current-sync/` 备查：

| 工作流 | 触发 | 职责 |
|--------|------|------|
| get_mesProductionData (35节点) | 每天1点 | 生产数据（含**日本 mes_jp**）→ production_detail+daily_info |
| get_mespackingdata (25节点) | 每天1点 | 包装数据 → pack_detail+daily_info |
| get_channel (55节点) | 每30分钟 | 渠道（国内+国外）→ delivery_detail+daily_info + 旧MES回写 |
| 同步管易数据到本地 (52节点) | 每6小时 | 管易deliverys→shop映射channel → mes_delivery_order_details |
| 同步唯一码到管易 (39节点) | form+定时 | SN推管易(uuid绑定) |
| mes_info (29节点) | webhook GET | coros后台拉取口（by-sn / by-time） |

**工序ID映射**：`39=uuid绑定  42=wifi_mac  31=battery  50=screen  59=remark  148=mac(备)  62=解绑  58=新增标记`

**关键发现**：日本数据已在 get_mesProductionData 中纳入（mes_jp.machine_data），无需额外处理。

### 目标架构设计 V1（已交付）

**核心设计**：三通道 + sync_task 状态机 + 对账补偿

1. **🔴 实时通道（新增）**：绑定(工序39)/解绑(工序62)末尾挂推送节点 → coros实时接口 → 解决出厂前锁机校验
2. **🟡 批量通道（改造）**：生产/包装/渠道保留定时但加状态登记 + 幂等 upsert，渠道依赖包装完成才触发
3. **sync_task 状态机（新增核心）**：retroid+sync_type 唯一，记录 PENDING/PROCESSING/SUCCESS/FAILED + retry_count + last_error → 解决"无法重试+无法发现"
4. **对账工作流（新增）**：定时扫缺渠道/缺wifimac等缺口 → 重入sync_task → 仍失败告警

**daily_info 结论**：保留小改不重做。表结构（宽表+外键三明细）合理，问题在填充机制而非表设计。

**交付物**：
- 本地：`workspace/sync_architecture_design_v1.md`
- 飞书：https://www.feishu.cn/docx/FubzdyBq0ogPLIx5RVjcZnxNn4e

### coros 实时推送接口（熊旺提供）

```
POST https://simpleadmincntest.coros.com/coros/admin/product/mes/push
Headers: signature(SHA1 40位hex) + timestamp(秒级) + nonce + Content-Type:application/json
Body: { "msg": [ {完整daily_info记录} ] }  ← msg是数组，可批量
```

**关键架构启示**：这是 PUSH 模式接口，接收完整 daily_info 记录。→ 实时通道可统一成：任何数据就绪 → 组装该SN的daily_info记录 → push。同一SN可多次push增量补全，coros按retroid更新。

**⚠️ 落地阻塞点**：签名算法未提供！signature=SHA1(?) 需要熊旺提供签名拼接规则+secret密钥。

### 5 个待确认（设计落地前必须）

1. coros 实时推送接口文档（地址/入参/支持类型/幂等约定/鉴权）
2. 绑定/解绑的 n8n 工作流（现在哪几个流程写工序39/62？）
3. 渠道"包装后触发"的判断方式（包装完成有没有明确事件/状态位？）
4. 旧MES回写范围（是否所有 channel 都要回写 consm_barcodebindinfo？日本数据要回写吗？）
5. 重试/对账频率（可接受的延迟？）

**状态**：等熊旺评审设计稿 + 回答 5 个待确认。上一轮 run 被 abort 后重新发起，已交付 V1 设计稿。

---

## 📊 MES→coros后台 同步逻辑全景（彻底理清）

coros后台不直读MES生产库，读中转 schema `mes_daily_data`（1主表+3明细）：

```
daily_info(主表)
  ├─ production_id → production_detail（生产数据）
  ├─ delivery_id   → delivery_detail（含channel）
  └─ pack_id       → pack_detail（包装数据）
```

三条定时喂数任务：
- **get_mesdailydata**（每天1点）：源 mes.machine_data(procedure_detail_id=58等)+joborder → 写 production_detail+daily_info
- **get_mespackingdata**（每天1点）：包装工序 → 写 pack_detail+daily_info
- **get_channel**（每天3点）：国内mes_delivery_order_details+国外OMS snoutbounds → 写 delivery_detail+daily_info（channel在此步进来）

coros后台拉取入口：mes_info（webhook POST /mes_info，带sn查）→ daily_info LEFT JOIN 三明细

**渠道问题根治链**：管易同步 → 灌mes_delivery_order_details → get_channel → daily_info → coros后台
